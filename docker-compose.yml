services:
  admin:
    build: .
    image: irms-fe-admin
    container_name: irms-fe-admin
    ports:
      - "3002:3002"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
    networks:
      - irms_net

networks:
  irms_net:
    name: irms_net
